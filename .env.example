# ── API keys (required on Render) ───────────────────────────────────────────
# GROQ_API_KEY=
# FIRECRAWL_API_KEY=
# GEMINI_API_KEY=
# LLM_PROVIDER=gemini

# ── CORS — browser origin(s) allowed to call the API (comma-separated) ───────
ALLOWED_ORIGIN=https://dental-price-matcher.vercel.app
