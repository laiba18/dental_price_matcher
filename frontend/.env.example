# Backend API URL (leave empty to use Vite dev proxy at /api → http://127.0.0.1:8000)
# Production build uses .env.production (see vite.config.ts port 3000 for local dev)
VITE_API_URL=https://dental-price-matcher-fv3x.onrender.com
